/*
Задача 3.  
Напишите программу, 
которая запрашивает ввод с клавиатуры слова (строки). 
Программа должна сформировать новую строку 
из введенной строки, 
поставив символы исходной строки в обратном порядке, 
и вывести новую строку на экран.

Пример работы программы:
Входные данные:
word
Выходные данные:
drow
*/ 

#include <iostream>
#include <string>

int main()
{
    setlocale(LC_ALL, "ru");
    std::cout << "Введите текст:\n";
    std::string s1;
    getline(std::cin, s1);
    int len = s1.length();

    for (int i = len-1; i>= 0; i--) 
    {
        std::cout << s1[i];
    }
}
