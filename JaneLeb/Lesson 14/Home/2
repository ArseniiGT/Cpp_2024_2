/*
Задача 2.
Напишите программу, 
которая запрашивает ввод с клавиатуры слова (строки). 
Программа шифрует слово следующим образом: 
“первая буква-последняя буква слова” 
и выводит шифр.

Пример работы программы:
Входные данные:
Hello
Выходные данные:
H-o
*/ 

#include <iostream>
#include <string>

int main()
{
    setlocale(LC_ALL, "ru");
    std::cout << "Введите текст:\n";
    std::string s1;
    getline(std::cin, s1);
    int len = s1.length();
    std::cout << s1[0] << "-" << s1[len - 1] << std::endl;
}
