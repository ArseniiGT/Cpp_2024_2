/*
Напишите программу, 
которая запрашивает ввод с клавиатуры двух строковых значений 
(в новых строках). 
Программа должна склеить введенные строки, 
вставив между ними пробел 
и вывести на экран.
*/ 

#include <iostream>
#include <string>
int main()
{
    setlocale(LC_ALL, "ru");
    std::string s1;
    std::string s2;
    std::cout << "Введите первую строку:\n";
    std::cin >> s1;
    s1 = s1 + " ";
    std::cout << "Введите вторую строку:\n";
    std::cin >> s2;
    std::cout << s1+s2 << std::endl;
}
